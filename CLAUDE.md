# BoatAI - プロジェクト概要

## これは何か
ボートレース予想支援AIアプリ。自分＋少数の友人向け（不特定多数への公開はしない）。
出走表・直前情報・オッズ・記者予想・JLCデータ予想などを入力すると、AIが3連単の買い目を予想・提案する。
単なる記録ツールではなく、ユーザー独自の分析ルール（教訓）を蓄積し、レースを重ねるほどAI予想の精度を育てていくことがコア価値。

詳細は `docs/01_proposal.md`、`docs/02_requirement_definition.md`、`docs/03_detailed_design.md` を参照。

## 技術スタック
- フロントエンド: React（PWA対応、レスポンシブ、モバイルファースト）
- バックエンド: Python（FastAPI）
- DB: Supabase（PostgreSQL）
- AI連携: Claude API
- デプロイ: Vercel（フロント）／Render（バックエンド）／Supabase（DB）、すべて無料枠

## 重要な設計方針（変更する場合は要確認）
- **AI予想は3段階で更新する**: 出走表確定時点 → 直前情報公開時点 → 締切直前（最終オッズ確定時点）。
  各段階の予想はPREDICTIONSテーブルに履歴として保存し、上書きしない。
- **買い目は3連単、5点（200円×5点＝1,000円）が基本**。他の式別は今回スコープ外。
- **ルールはユーザーごとに管理**し、`is_active`フラグでAI予想プロンプトへの反映有無を切り替える。ルールは自然文のテキストとして保存する。
- **収支は「実際の買い目」と「AI提案通りの買い目」の両方をシミュレーションし、比較できるようにする**（BETSテーブルの`is_ai_suggested`で区別）。
- **レース種別（一般戦／SG等）でタグ付け**し、ルールの精度を種別ごとに集計できるようにする。一般戦は開催数が多く、ルール検証の主戦場として扱う。

## データベース設計
ER図は `docs/03_detailed_design.md` のMermaid図を正とする。主なテーブル：
`USERS` / `RACES` / `RACE_ENTRIES` / `PREDICTIONS` / `PREDICTION_RULES` / `RULES` / `BETS` / `RESULTS`

## スコープアウト（今回は実装しない）
- 出走表・オッズ等の自動スクレイピング（今回は手動入力。将来拡張として設計だけ考慮）
- SNS的な公開機能（フォロー、タイムライン、いいね等）
- 課金・決済機能（実際の金銭のやり取りは発生しない）
- 3連単以外の式別（複勝・2連単等）

## セキュリティ要件
- パスワードはbcryptでハッシュ化。平文保存禁止。
- 認証はJWTベースのセッション管理。
- 全APIエンドポイントでユーザーIDに基づくデータアクセス制御を行い、他ユーザーのデータへのアクセスを禁止する。
- Claude APIキーはバックエンドの環境変数で管理し、フロントエンドに露出させない。
- ORM（SQLAlchemy等）を使用し、生SQLの文字列結合は行わない。
- 全エンドポイントでPydanticモデル等によるリクエストバリデーションを実施する。

## コーディング規約・進め方の好み
- 実装計画（マイルストーン、実装ステップ）を提示する際は、必ず作業前にステップを箇条書きで確認してから進める。
- 破壊的な変更（DBスキーマ変更、既存ファイルの大幅書き換えなど）を行う前は必ず確認を取る。
- レベル1（必須機能）から順に実装し、レベル2・3には手を広げない。詳細は`docs/02_requirement_definition.md`の機能レベル分類を参照。

## 参照ドキュメント
- `docs/01_proposal.md` : 要望書
- `docs/02_requirement_definition.md` : 要件定義書（機能レベル分類・スコープアウト）
- `docs/03_detailed_design.md` : 詳細設計書（ER図・画面遷移図・セキュリティ仕様）
- `docs/04_design_report.md` : 設計中間報告書
- `docs/05_deployment.md` : デプロイ手順書（Supabase / Render / Vercel）
